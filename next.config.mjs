/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["aceternity.com", "https://lh3.googleusercontent.com"], // Add the allowed image domain
  },
};

export default nextConfig;
